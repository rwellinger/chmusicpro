# Development override - build locally instead of pulling from registry
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:
  chmusicpro-app:
    build:
      context: .
      target: app
    pull_policy: build
    image: chmusicprosrv-app:local
